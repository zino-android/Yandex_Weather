<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/city_not_selected_relative_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:visibility="invisible">

    <ImageView
        android:id="@+id/robot_image_view"
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:layout_centerInParent="true"
        android:adjustViewBounds="true"
        android:scaleType="fitCenter"
        android:src="@drawable/robot" />

    <TextView
        android:id="@+id/text_error_text_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/robot_image_view"
        android:layout_margin="16dp"
        android:gravity="center"
        android:text="@string/city_not_selected"
        android:textAppearance="@style/WeatherTextViewStyle" />

    <Button
        android:id="@+id/add_city_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/text_error_text_view"
        android:layout_margin="16dp"
        android:background="@drawable/add_city_button_selector"
        android:text="@string/add_city_button"/>

</RelativeLayout>